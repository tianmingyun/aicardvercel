{
  "buildCommand": "npm run build",
  "installCommand": "npm install --force",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": {
        "buildCommand": "npm run build",
        "installCommand": "npm install --force"
      }
    }
  ]
}